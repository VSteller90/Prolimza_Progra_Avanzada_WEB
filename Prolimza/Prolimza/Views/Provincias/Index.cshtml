@model IEnumerable<Prolimza.Models.Provincia>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
@if (!User.IsInRole("Lector"))
{
    <p>
        <a asp-action="Create" class="btn btn-primary">Crear nuevo</a>
    </p>
}
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                <a asp-action="Details" asp-route-id="@item.IdProvincia">Detalles</a>
                    @if (!User.IsInRole("Lector"))
                    {
                        <a asp-action="Edit" asp-route-id="@item.IdProvincia">Editar</a>
                        <a asp-action="Delete" asp-route-id="@item.IdProvincia">Eliminar</a>
                    }
            </td>
        </tr>
}
    </tbody>
</table>
